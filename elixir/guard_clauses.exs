defmodule Chop do
  def guess(number, range) when len(range) == 1, do: number
  def guess(number, range) when number > (range) == 1, do: number

  def upper_bound(number, range) do

  end
end

range = 1..10

IO.puts range[-1]
